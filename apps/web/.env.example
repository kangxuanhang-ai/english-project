# 前端环境变量示例
# 复制为 apps/web/.env.development 或 .env.production

# MinIO 头像上传/预览基址（与后端 MINIO 对外访问一致）
VITE_MINIO_ENDPOINT=http://localhost:9000

# Socket.IO 连接地址（开发可留空走 Vite 同源；生产填完整 wss:// 或 https://）
VITE_SOCKET_URL=http://localhost:3000
